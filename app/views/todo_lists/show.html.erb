
<div class="ccontainer">
<h2><%= @todo_list.title %></h2><br>
<div class="items-list">
  <% @todo_list.items.each do |item| %>
    <%= link_to item.content, class:"items" %><br>
  <% end %>
</div>
<%#
  <%= link_to "Je m'occupe de #{item.content}", mark_as_done_todo_list_item_path(@todo_list, item), method: :patch %>
<%= render "shared/buttons/validate_btn" %>
</div>
